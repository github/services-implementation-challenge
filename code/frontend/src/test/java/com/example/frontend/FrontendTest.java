package com.example.frontend;

import junit.framework.TestCase;

public class FrontendTest extends TestCase {

        public FrontendTest(String name) {
                super( name );
        }

        public void testFrontend() throws Exception {
            // Don't run this yet - just a placeholder...
            assertTrue(false);
        }
}